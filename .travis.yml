language: java
script:
    ./gradlew pTML && pushd test && ../gradlew pTML && popd && pushd testConsume && ../gradlew pTML && popd
after_success: git fetch && git checkout master && git pull origin master && git merge develop && git push origin master
